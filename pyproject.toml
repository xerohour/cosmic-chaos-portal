[project]
name = "cosmic-chaos-portal"
version = "0.1.0"
description = "Milestone 1: multi-agent ritual AI core — Archon orchestrator, typed agent contracts, safety policy engine"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
